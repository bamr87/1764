<!--
  ROADMAP.md — the forward plan (what to grow next). Maintained by the `plan-roadmap` skill on every
  `/grow` tick. Items are tagged: content | structure | meta. The architect picks from **Now** /
  **Backlog**; completed items move to **Done**; newly discovered work lands in **Ideas**.
  Hand-edits are allowed but will be reconciled on the next tick.
-->

# Roadmap

Forward plan for growing this knowledge base. Concept and current state live in [seed.md](seed.md).

## Now
_Selected by the last `plan-roadmap` run for the current tick._

- _(none yet — run `/grow` to populate)_

## Backlog
_Prioritized queue. Top items are pulled into **Now** each tick._

1. `structure` — Generate category index pages `<category-slug>/index.md` for each taxonomy entry.
2. `structure` — Generate `TIMELINE.md` (subject is time-oriented) from the knowledge table + topic files.
3. `structure` — Generate `INDEX.md` (or a README TOC) linking every topic file by category.
4. `content` — Deep-dive the most notable existing table rows into dedicated files (start with the California Gold Rush, Elizabeth Blackwell, the Roman Republic).
5. `content` — Fill under-represented categories up to ≥3 rows each (Economics and Arts & Culture are thinnest).
6. `structure` — Add cross-reference "Related" links between topic files.
7. `meta` — Run `/evolve` to audit the customization layer (every ~5th tick).

## Done
_Recently completed, newest first (with tick date)._

- _(none yet)_

## Ideas
_Discovered follow-ups, unscored. Promote to Backlog when ready._

- Consider a published static site / TOC once enough topic files exist.
- Periodically re-derive `concept.taxonomy` as coverage reveals new dimensions (e.g. the 1848–49 revolutions may warrant a dedicated dimension).
- Cross-link people who died in 1849 (Chopin, Poe) with their Arts & Culture works.
